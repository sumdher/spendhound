global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: spendhound_backend
    metrics_path: /metrics
    static_configs:
      - targets:
          - backend:8000
    authorization:
      type: Bearer
      credentials: dev-metrics-token
